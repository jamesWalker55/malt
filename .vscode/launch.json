{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Reaper",
      "cargo": {
        "args": ["build"],
        "filter": {
          "name": "malt",
          "kind": "bin"
        }
      },
      "cwd": "${workspaceFolder}"
    }
  ]
}
